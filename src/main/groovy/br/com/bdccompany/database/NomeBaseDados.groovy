package br.com.bdccompany.database

class NomeBaseDados {

    def static getRegistroValue(cpf) {
        //vai estar com erro porque não tem a configuração de conexão com o banco
       // def result = NomeDaConexaoDoBanco.firstRow("SELECT * FROM NOME_TABELA WHERE CPF  = ${cpf}")
      //  return result.CPF //onde CPF é o nome da coluna que retorna
    }
}
